# INDEX

> Auto-generated by `Tools/gen-index.ps1`. Do not edit by hand -- edit the script
> header comment (or add a `// INDEX: ...` line) and re-run the generator.

_41 scripts under Assets/Scripts._

## Beatmaps

| Script | Description |
| --- | --- |
| `Beatmap.cs` | Parsed .osu data model — General/Metadata/Difficulty/Events sections plus the hit-object list. |
| `BeatmapParser.cs` | Parses the osu! (.osu) file format, standard mode (Mode 0) |
| `DifficultyCalculator.cs` | Converts the abstract difficulty settings (CS / AR / OD / HP) into concrete gameplay values, and processes a beatmap (combo colours, slider timing) after parsing |
| `HitObjects.cs` | Hit-object model + type/hitsound bitfields — HitObject base and Circle/Slider/Spinner classes. |
| `SliderPath.cs` | Computes the geometry of a slider from its control points and resamples it to a fixed pixel length |
| `TimingPoint.cs` | A single line from the [TimingPoints] section |

## Gameplay

| Script | Description |
| --- | --- |
| `AssetLoader.cs` | Loads audio and background images from a beatmap folder at runtime |
| `BackgroundDim.cs` | Camera-space black quad that dims everything behind gameplay (video, skybox, far scene). |
| `BeatmapDownloader.cs` | Downloads .osz archives from public osu! mirrors (no auth/API key required) |
| `BeatmapLibrary.cs` | Lightweight star-rating estimate (NOT osu!'s official SR — a note-density proxy nudged by AR/OD, computed cheaply at scan time) used to order difficulties easiest → hardest |
| `Bootstrap.cs` | Entry point — loads skin, scans BeatmapLibrary, drives SongSelectUI, then hands off to GameManager. |
| `CursorController.cs` | Projects the mouse onto the playfield plane and tracks osu!-style input |
| `FirstPersonCamera.cs` | First-person mouse-look for the sphere-chunk view |
| `GameClock.cs` | Audio-synced clock (dspTime) reporting song time in ms, with lead-in and pause support. |
| `GameContext.cs` | Shared state & tuned values handed to every drawable hit object |
| `GameManager.cs` | Drives a single play session: builds the playfield, spawns hit objects in time with the audio, resolves input (with osu! note-lock ordering) and renders the HUD |
| `GameSettings.cs` | Runtime-adjustable, persisted game settings |
| `HitSoundPlayer.cs` | Plays hit sounds |
| `HudSkin.cs` | Draws the gameplay HUD (score / combo / accuracy fonts + scorebar health) from the osu! skin. |
| `Osu3DSettings.cs` | Optional scene-level tuning for the 3D cylinder view |
| `OszImporter.cs` | Extracts a .osz archive (a renamed .zip) to a working directory |
| `Playfield.cs` | Maps osu! playfield coordinates (512x384, origin top-left, y down) into 3D world space |
| `ScoreProcessor.cs` | Tracks score, combo, accuracy and HP |
| `SongSelectUI.cs` | osu!lazer-style song select UI (carousel, search/sort, difficulty panel, download-by-id), built at runtime via uGUI. |
| `VideoPlayback.cs` | Plays a beatmap's background video as a camera-space backdrop quad, synced to GameClock. |
| `ViewModeController.cs` | Runtime view-mode switcher — configures camera+projection per ViewMode, toggles mid-map. |

## Skinning

| Script | Description |
| --- | --- |
| `Skin.cs` | A loaded osu! skin — folder of named .png elements + parsed skin.ini, with lazy @2x textures. |
| `SkinConfig.cs` | Parsed skin.ini values (only the subset this game uses) |
| `SkinNumber.cs` | Renders a run of digits (combo numbers) with the skin's number font; TextMesh fallback. |
| `SkinSprites.cs` | Resolves a gameplay element to the current skin's sprite, falling back to the procedural art when no skin is active or the element is missing |

## Util

| Script | Description |
| --- | --- |
| `ArchiveExtractor.cs` | Extracts a zip-based archive (.osz beatmap, .osk skin — both are plain .zip files) to a folder |
| `MaterialFactory.cs` | Shared unlit/transparent materials used for procedurally created meshes (sliders) |
| `TextureFactory.cs` | Generates the simple circle / ring sprites used by the gameplay so the project needs no imported art assets |
| `WavDecoder.cs` | Minimal RIFF/WAVE PCM decoder (raw bytes to AudioClip) bypassing Unity's FMOD loader. |

## Visual

| Script | Description |
| --- | --- |
| `DrawableHitObject.cs` | Base type for the on-screen representation of a hit object |
| `FloatingText.cs` | A short-lived judgement result (skin animation or 3D text) that pops and fades upward |
| `FollowPoints.cs` | Follow points — the fading line of arrows guiding the eye from one object to the next. |
| `HitCircleObject.cs` | Drawable hit circle — body/overlay/approach-circle/combo-number, judgement + fade animation. |
| `SliderObject.cs` | Drawable slider — body/border line, head, follow circle, ticks, reverse arrows, ball tracking. |
| `SpinnerObject.cs` | Drawable spinner — spinning rotor, rotation accumulation, progress ring and clear flash. |
| `VisualResources.cs` | Lazily-loaded shared visual resources (the built-in runtime font for 3D text) |


