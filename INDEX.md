# INDEX

> Auto-generated by `Tools/gen-index.ps1`. Do not edit by hand -- edit the script
> header comment (or add a `// INDEX: ...` line) and re-run the generator.

_65 scripts under Assets/Scripts._

## Beatmaps

| Script | Description |
| --- | --- |
| `Beatmap.cs` | Parsed .osu data model — General/Metadata/Difficulty/Events sections plus the hit-object list. |
| `BeatmapParser.cs` | Parses the osu! (.osu) file format, standard mode (Mode 0) |
| `DifficultyCalculator.cs` | Converts the abstract difficulty settings (CS / AR / OD / HP) into concrete gameplay values, and processes a beatmap (combo colours, slider timing) after parsing |
| `HitObjects.cs` | Hit-object model + type/hitsound bitfields — HitObject base and Circle/Slider/Spinner classes. |
| `SliderPath.cs` | Computes the geometry of a slider from its control points and resamples it to a fixed pixel length |
| `TimingPoint.cs` | A single line from the [TimingPoints] section |

## Editor

| Script | Description |
| --- | --- |
| `SymbolFallbackFontSetup.cs` | Builds the DejaVu Sans TMP font asset and registers it as a global TMP fallback |

## Gameplay

| Script | Description |
| --- | --- |
| `AssetLoader.cs` | Loads audio and background images from a beatmap folder at runtime |
| `AutoPilot.cs` | Autoplay driver — walks the map timeline and emits cursor position + tap state each frame. |
| `BackgroundDim.cs` | Camera-space black quad that dims everything behind gameplay (video, skybox, far scene). |
| `BeatmapDownloader.cs` | Public osu! mirror client (no auth) — .osz download by set id (osu.direct→catboy) plus text search returning osu!-API-shaped beatmapset metadata for the song-select Online tab (U5). Mirrors must accept TLS 1.2: Unity 2022 can't do 1.3. |
| `BeatmapLibrary.cs` | Lightweight star-rating estimate (NOT osu!'s official SR — a note-density proxy nudged by AR/OD, computed cheaply at scan time) used to order difficulties easiest → hardest |
| `Bootstrap.cs` | Entry point — loads skin, scans BeatmapLibrary, drives SongSelectUI, then hands off to GameManager. |
| `BreakSkip.cs` | Click-free gap detection (song intro + mid-map breaks) plus the skip overlay: bar, countdown, skip button. |
| `CursorController.cs` | Projects the mouse onto the playfield plane and tracks osu!-style input |
| `FirstPersonCamera.cs` | First-person mouse-look for the sphere-chunk view |
| `GameClock.cs` | Audio-synced clock (dspTime) reporting song time in ms, with lead-in and pause support. |
| `GameContext.cs` | Shared state & tuned values handed to every drawable hit object |
| `GameManager.cs` | Drives a single play session: builds the playfield, spawns hit objects in time with the audio, resolves input (with osu! note-lock ordering) and renders the HUD |
| `GameSettings.cs` | Runtime-adjustable, persisted game settings |
| `HitSoundPlayer.cs` | Plays hit sounds and the combo-break sound |
| `HudSkin.cs` | Draws the gameplay HUD (score / combo / accuracy fonts + scorebar health) from the osu! skin. |
| `Osu3DSettings.cs` | Optional scene-level tuning for the 3D cylinder view |
| `OszImporter.cs` | Extracts a .osz archive (a renamed .zip) to a working directory |
| `Playfield.cs` | Maps osu! playfield coordinates (512x384, origin top-left, y down) into 3D world space |
| `ScoreProcessor.cs` | Tracks score, combo, accuracy and HP |
| `SongSelectUI.cs` | Song select on the U1 UI kit — Local/Online sources (mirror search + download→auto-import), set carousel + detail panel, audio preview (PreviewTime local / ppy preview online), star/length/BPM filters + sort (defaults to Date Added), type-to-search, full keyboard nav, glance metadata (CS/AR/OD/HP/len/BPM). Built at runtime via uGUI+TMP. |
| `VideoPlayback.cs` | Plays a beatmap's background video as a camera-space backdrop quad, synced to GameClock. |
| `ViewModeController.cs` | Runtime view-mode switcher — configures camera+projection per ViewMode, toggles mid-map. |

## Skinning

| Script | Description |
| --- | --- |
| `Skin.cs` | A loaded osu! skin — folder of named .png elements + parsed skin.ini, with lazy @2x textures. |
| `SkinConfig.cs` | Parsed skin.ini values (only the subset this game uses) |
| `SkinNumber.cs` | Renders a run of digits (combo numbers) with the skin's number font; TextMesh fallback. |
| `SkinSprites.cs` | Resolves a gameplay element to the current skin's sprite, falling back to the procedural art when no skin is active or the element is missing |

## UI

| Script | Description |
| --- | --- |
| `MainScreen.cs` | Fallcall main screen — title + Play/Browse/Settings/Quit entries, each showing its keyboard shortcut, with graceful first-run (no maps → Browse, not a dead Play). Routes via the NavBar-shared MenuRoute (U3). |
| `NavBar.cs` | Fallcall persistent navigation toolbar — a toggleable top strip (Ctrl+T) giving the same routes as the main screen from anywhere, plus a shared MenuRoute enum, transient toast, and the UiInput typing-guard helper (U3). |
| `PauseMenu.cs` | The pause menu — Continue / Restart / Quit and nothing else. Settings live in the global overlay (Ctrl+O), not in here. |
| `SettingsOverlay.cs` | Fallcall settings overlay (U2) — slide-over panel openable anywhere (Ctrl+O), sidebar sections + search-with-highlight, live-applying sliders/toggles with per-setting + per-section reset, and a rebindable keybinds section with conflict detection. Backed by GameSettings; built from the U1 UiKit. |
| `UiCoverCache.cs` | Shared list-artwork loader — fetches every listed map's card image up front (mirror covers over HTTP, .osz backgrounds off disk) through a small request window, downscales each to thumbnail size, and keeps an LRU of the decoded textures so re-listing a result set costs nothing. |
| `UiCoverFit.cs` | Cover-fill for a RawImage — crops its uvRect so artwork fills the rect without stretching, recomputed whenever the rect or the texture changes. What lets one map card shape hold both a 800×280 mirror cover and a 1920×1080 .osz background. |
| `UiKit.cs` | Fallcall shared UI widget kit — runtime-built uGUI+TMP factory (canvas, panel, button, toggle, range slider, search field, section header, list row) with hover/press/keyboard-focus states baked in. Every menu draws from this (U1). |
| `UiMapCard.cs` | The map card — the one list element behind both map selection and map search: the map's artwork bleeding to the card's rounded edges, a scrim over it that doubles as the hover tint, and title/subtitle sitting on the scrim. Built from the U1 kit; a developer prefab carrying the same UiRow slots supersedes it. |
| `UiPlaceholder.cs` | Edit-time placeholder marker — tags sample nodes (preview canvas, sample list rows) the build creates so they can be auto-removed before real data binds / on entering Play. Own file because it can be scene/prefab-serialized. |
| `UiRow.cs` | List-row presentation contract — named slots (button/content/title/subtitle/marker) the screen binds data through. Lives on both the code-default row and a developer's styled row prefab, so rows can be restyled in the editor without touching gameplay code. Own file because it is prefab-serialized. |
| `UiScaffold.cs` | Idempotent UI reconcile kit — get-or-create nodes/components so a screen can be built from the editor and re-synced without duplicating or clobbering hand edits. Also the list-row contract (UiRow) + placeholder-then-data list binder (UiListView) that lets developers style beatmap/difficulty rows as prefabs. |
| `UiTheme.cs` | Fallcall UI design tokens — palette, typography scale, spacing/radii, motion + shared TMP font and rounded-rect sprite resources. The single source of styling for the UI kit (U1). |

## UI\Browser

| Script | Description |
| --- | --- |
| `MapBrowser.cs` | The map browser screen — owns browse state (results → filter/sort → selection), drives the chrome, search, and media, and turns Enter into either a mirror download+auto-import or a play request. Built at runtime, keyboard-first (U6). |
| `MapBrowserMedia.cs` | Map browser media — streams the ppy audio demo (Ogg/Vorbis despite the .mp3 URL) and fetches set covers, both debounced + guarded against a stale response landing after the player moved on (U6). |
| `MapBrowserModel.cs` | Map browser data model — the mirror-search result shape (BrowseSet/BrowseDiff), the client-side filter/sort rules, and every string the browser renders. Pure C#, no uGUI, so the layout and the screen can both read it (U6). |
| `MapBrowserRows.cs` | Map browser list plumbing — the code-default map/difficulty rows (superseded by a developer's row prefab), the selection marker, and the scroll-content helpers the browser's two lists share (U6). |
| `MapBrowserSearch.cs` | Map browser search driver — debounces typing, calls the mirrors via BeatmapDownloader, drops superseded responses and hands back a BrowseSet list (osu!standard only). Owns no UI (U6). |
| `MapBrowserView.cs` | Map browser chrome — builds the browse screen (results column left; search+filters top-right; map detail with the reserved preview slot bottom-right) and hands back every widget reference. Construction only, no behaviour (U6). |
| `MapBrowserWords.cs` | Inline word-filter row for the map browser — a fixed heading followed by clickable words (osu! listing style), single- or multi-select, that highlight the active choice. Construction + selection state only (U6). |

## Util

| Script | Description |
| --- | --- |
| `ApiLog.cs` | Tracing for outbound HTTP (mirror search, .osz download, ppy CDN preview/cover) — logs each request's start and outcome with status, size and elapsed time to a per-session file under persistentDataPath/logs, and to the editor console. |
| `ArchiveExtractor.cs` | Extracts a zip-based archive (.osz beatmap, .osk skin — both are plain .zip files) to a folder |
| `MaterialFactory.cs` | Shared unlit/transparent materials used for procedurally created meshes (sliders) |
| `RenderOrder.cs` | The one place the whole draw stack is defined — render queues, sorting orders and canvas orders for every band from the skybox up to the settings overlay. |
| `TextureFactory.cs` | Generates the simple circle / ring sprites used by the gameplay so the project needs no imported art assets |
| `WavDecoder.cs` | Minimal RIFF/WAVE PCM decoder (raw bytes to AudioClip) bypassing Unity's FMOD loader. |

## Visual

| Script | Description |
| --- | --- |
| `DrawableHitObject.cs` | Base type for the on-screen representation of a hit object |
| `FloatingText.cs` | A short-lived judgement result (skin animation or 3D text) that pops and fades upward |
| `FollowPoints.cs` | Follow points — the fading line of arrows guiding the eye from one object to the next. |
| `HitCircleObject.cs` | Drawable hit circle — body/overlay/approach-circle/combo-number, judgement + fade animation. |
| `SliderObject.cs` | Drawable slider — body/border line, head, follow circle, ticks, reverse arrows, ball tracking. |
| `SpinnerObject.cs` | Drawable spinner — spinning rotor, rotation accumulation, progress ring and clear flash. |
| `VisualResources.cs` | Lazily-loaded shared visual resources (the built-in runtime font for 3D text) |


